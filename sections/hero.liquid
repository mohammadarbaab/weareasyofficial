
<div class="hero-main-wrapper">
<div class="inside-hero-wrapper">

  <!-- HERO TEXT OVERLAY -->
  <div class="custom-hero__content">
    <h1>Premium Trousers & T-Shirts Collection</h1>
    <p>Simple. Stylish. Comfortable. Discover your new everyday essentials.</p>
    <div class="hero-buttons">
      <a href="/collections/all" class="custom-hero__btn">WOMEN</a>
    <a href="/collections/all" class="custom-hero__btn">MEN</a>
    </div>
  </div>

  <!-- IMAGE 1 (HIDE ON MOBILE) -->
  <div class="hero-img hide-mobile">
    <img src="{{ 'hero-model-1.png' | asset_url }}" alt="Hero Image 1">
  </div>

  <!-- IMAGE 2 -->
  <div class="hero-img">
    <img src="{{ 'hero-model-2.png' | asset_url }}" alt="Hero Image 2">
  </div>

  <!-- IMAGE 3 -->
  <div class="hero-img">
    <img src="{{ 'hero-model-3.png' | asset_url }}" alt="Hero Image 3">
  </div>

</div></div>
<style>
  /* MAIN WRAPPERS */
.hero-main-wrapper {
  width: 100%;
  background-color: #fafafa;
}

.inside-hero-wrapper {
  display: flex;
  position: relative;
  width: 100%;
  min-height: 60vh;
  max-width: 1600px;
  margin: 0 auto;
}

/* HERO IMAGES */
.hero-img {
  flex: 1;
  overflow: hidden;
}

.hero-img img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  display: block;
  transition: transform 1.2s cubic-bezier(0.25, 0.46, 0.45, 0.94);
}

/* Optional: Subtle zoom on hover */
.hero-img:hover img {
  transform: scale(1.03);
}

/* TEXT OVERLAY - Minimal blur, mostly transparent */
.custom-hero__content {
  position: absolute;
  inset: 0;
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  text-align: center;
  color: #1a1a1a;
  padding: 24px;
  z-index: 10;
  /* Very subtle overlay - almost transparent */
  background: rgba(255, 255, 255, 0.05);
  /* Minimal blur effect */
  backdrop-filter: blur(1px);
}

/* TEXT STYLES */
.custom-hero__content h1 {
  font-size: clamp(24px, 4vw, 48px);
  font-weight: 400;
  letter-spacing: 3px;
  margin-bottom: 16px;
  color: #1a1a1a;
  text-transform: uppercase;
  line-height: 1.2;
  max-width: 800px;
  font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
  /* Better text shadow for readability on light backgrounds */
  text-shadow: 0 2px 4px rgba(255, 255, 255, 0.5);
}

.custom-hero__content p {
  font-size: clamp(14px, 1.5vw, 16px);
  margin-bottom: 28px;
  color: #2a2a2a;
  font-weight: 400;
  letter-spacing: 0.5px;
  max-width: 600px;
  /* Light text shadow for better visibility */
  text-shadow: 0 1px 3px rgba(255, 255, 255, 0.5);
}

/* BUTTON CONTAINER */
.hero-buttons {
  display: flex;
  gap: 20px;
  justify-content: center;
  flex-wrap: wrap;
}

/* PROFESSIONAL BUTTON STYLING */
.custom-hero__btn {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  min-width: 160px;
  height: 52px;
  padding: 0 32px;
  
  /* Subtle, sophisticated style */
  background-color: transparent;
  color: #1a1a1a;
  border: 1.5px solid #1a1a1a;
  
  font-size: 13px;
  font-weight: 500;
  letter-spacing: 2px;
  text-transform: uppercase;
  text-decoration: none;
  
  transition: all 0.4s cubic-bezier(0.25, 0.46, 0.45, 0.94);
  cursor: pointer;
  pointer-events: auto;
  position: relative;
  overflow: hidden;
  z-index: 1;
}

/* Modern hover effect */
.custom-hero__btn::before {
  content: '';
  position: absolute;
  top: 0;
  left: -100%;
  width: 100%;
  height: 100%;
  background-color: #1a1a1a;
  transition: left 0.4s cubic-bezier(0.25, 0.46, 0.45, 0.94);
  z-index: -1;
}

.custom-hero__btn:hover {
  color: #ffffff;
  border-color: #1a1a1a;
}

.custom-hero__btn:hover::before {
  left: 0;
}

/* Alternative filled button style - for primary CTA */
.custom-hero__btn.primary {
  background-color: #1a1a1a;
  color: #ffffff;
  border-color: #1a1a1a;
}

.custom-hero__btn.primary::before {
  background-color: #ffffff;
}

.custom-hero__btn.primary:hover {
  color: #1a1a1a;
  border-color: #1a1a1a;
}

/* ACTIVE STATE */
.custom-hero__btn:active {
  transform: scale(0.98);
}

/* FOCUS STATE (ACCESSIBILITY) */
.custom-hero__btn:focus-visible {
  outline: none;
  box-shadow: 0 0 0 3px rgba(26, 26, 26, 0.15);
}

/* MOBILE HIDE */
.hide-mobile {
  display: none;
}

/* TABLET & DESKTOP */
@media (min-width: 768px) {
  .hide-mobile {
    display: block;
  }

  .inside-hero-wrapper {
    min-height: 80vh;
  }
  
  /* Even less blur on desktop */
  .custom-hero__content {
    background: rgba(255, 255, 255, 0.02);
    backdrop-filter: blur(0.5px);
  }
}

/* MOBILE OPTIMIZATION */
@media (max-width: 767px) {
  .hero-buttons {
    flex-direction: column;
    gap: 12px;
    width: 100%;
    max-width: 280px;
  }

  .custom-hero__btn {
    width: 100%;
    height: 48px;
    min-width: auto;
    font-size: 12px;
    border-width: 1px;
  }

  .custom-hero__content h1 {
    font-size: 20px;
    letter-spacing: 2px;
    margin-bottom: 8px;
    text-shadow: 0 1px 3px rgba(255, 255, 255, 0.5);
  }

  .custom-hero__content p {
    font-size: 13px;
    margin-bottom: 20px;
    padding: 0 20px;
    text-shadow: 0 1px 2px rgba(255, 255, 255, 0.5);
  }
  
  /* Minimal blur on mobile */
  .custom-hero__content {
    background: rgba(255, 255, 255, 0.03);
    backdrop-filter: blur(0.5px);
  }
}

/* Small mobile devices */
@media (max-width: 480px) {
  .custom-hero__content h1 {
    font-size: 18px;
  }
  
  .custom-hero__content p {
    font-size: 12px;
  }
  
  .hero-buttons {
    max-width: 240px;
  }
}
</style>